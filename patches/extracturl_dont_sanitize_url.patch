Index: github/mutt/extracturl/extract_url.pl
===================================================================
--- github.orig/mutt/extracturl/extract_url.pl
+++ github/mutt/extracturl/extract_url.pl
@@ -261,6 +261,7 @@ sub renderuri {
 }
 sub sanitizeuri {
 	my($uri) = @_;
+	return $uri;
 	if ($sanitize_reserved) {
 		$uri =~ s/([^a-zA-Z0-9_.~%:\/-])/sprintf("%%%X",ord($1))/egs;
 	} else {
