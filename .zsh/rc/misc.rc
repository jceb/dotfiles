# source cdargs bash binding
if [[ -e ~/.cdargs-bash.sh ]]; then
	source ~/.cdargs-bash.sh
elif [[ -e /usr/share/cdargs/cdargs-bash.sh ]]; then
	source /usr/share/cdargs/cdargs-bash.sh
fi

# vi: ft=zsh:tw=0:sw=4:ts=4
