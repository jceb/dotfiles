# My zsh configuration depends on the grml configuration. Execute the following
# command only when you don't have a system wide grml zsh config!
#
# wget -O ~/.zshrc http://git.grml.org/f/grml-etc-core/etc/zsh/zshrc
